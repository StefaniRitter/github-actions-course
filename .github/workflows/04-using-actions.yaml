name: 04 - Using Actions

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
        - name: Checkout Code
          uses: actions/checkout@v4
        - name: Printing Folders
          run: ls


    # Checkout de code from the repository
    # Install deps of react application
    # Execute automated tests